import { routes as auth } from '../views/pages/auth'//login e tudo que for relacionado a autenticacao
import { routes as dashboard } from '../views/pages/Dashboard'//login e tudo que for relacionado a autenticacao


export default [
  ...auth,
  ...dashboard
]